# 1636架构介绍

## 简介

1636是一款基于ARM V9指令集的通用CPU。1636是一款针对HPC进行了专项优化的鲲鹏处理器。

| 规格     |             | 指标                                     |
| -------- | ----------- | ---------------------------------------- |
| 基本规格 | ISA         | ARMV9 SVE2+SME                           |
|          | 核数        | 304                                      |
|          | SME主频     | 1.55G                                    |
|          | Scalar主频  | 2GHz                                     |
|          | L1D         | 32KB                                     |
|          | L1I         | 32KB                                     |
|          | Private L2  | 768KB                                    |
|          | Issue Width | 4                                        |
|          | 整芯片功耗  | 690W                                     |
|          | HBM容量     | 8*4G                                     |
|          | HBM带宽     | 8*0.5TBps                                |
|          | DDR通道     | 8                                        |
|          | DDR速率     | 5600Mbps                                 |
| SME算力  | FP64        | 60                                       |
|          | FP32        | 120                                      |
|          | FP16        | 240                                      |
|          | BF16        | 240                                      |
|          | INT8        | 960                                      |
| SVE算力  | FP64        | 17                                       |
|          | FP32        | 34                                       |
|          | FP16        | 68                                       |
|          | BF16        | 68                                       |
|          | INT8        | 136                                      |
| IO规格   | PCIe        | 4组x8(5.0)(每组x8和HCCS H32共用，二选一) |
|          | HCCS(H32)   | 4组x8(每组和PCIE互斥，二选一)            |
|          | HCCS(H60)   | 8组x4                                    |
|          | Network     | 4组 200G/100G/25G/10G                    |
|          | SATA        | 2组(3.0)                                 |
|          | USB         | 2组(2.0)                                 |



1636是一个使用复杂工艺封装的多DIE芯片，一共有14颗DIE，包含有2颗计算DIE，8颗HBM DIE，4颗IO DIE。其中，1颗计算DIE，4颗HBM DIE，和2颗IO DIE使用2.5D封装，放在一个基板上行程一个整体，称之为一个ICD。2个ICD通过MCM封装为一个1636芯片。

整芯片一共有304Core，被分成8个Cluster，每一个Cluster有38Core，通过目录结构(UC)维护cache一致性。Cluster内部的38个Core通过一个专门的片上环形总线(c-ring)连接在一起，并在两端有目录结构（UC）维护cache一致性。UC的后端是HBM颗粒。


## CORE 的设计
```Core 是兼容 ARM V9 ISA 的 Linx 架构 Core。流水线是 4 发射乱序核， 乱序资源和鲲鹏 920的 Core 相当。除了传统的 OoO 资源，Core 中集成了 SVE 和 SME 单元。SVE 单元长度为2*512bit，即有 2 条 pipeline, 每条均支持 512bit 宽度。SME 单元为 8*8 MAC，每一个 cycle支持 64 个 MAC 操作，即 128 Flops/cycle. Core 中带有 32KB 的 L1D 和 L1I，768KB 的 L2。为了适配 SVE 和 SME 的计算，LSU 单元进行了改造，L1D 支持每 cycle 2 个 64B load，1 个64B write。```


## SVE 算力
```SVE 单元是一个向量加速单元。因为每一个 Core 内均有 2 个 512bit 的 SVE 单元。每一个SVE 单元执行 SIMD 指令，每一个 cycle 最多可以同时计算 512/64=8 个双精度操作数，同时其支持乘累加两个操作，因此其浮点算力是 16Flops。每一个 Core 实现 32Flops/cycle.在 SVE 打开，SME 不使用的情况下，CPU 可以 turbo 到 1.65GHz，因此，整芯片的向量算力是 32Flops/cycle/core*1.75GHz*304Core=17TFlops。混合精度配比是FP64:FP32:FP16:int8=1:2:4:8。```


## SME 算力
```SME 是一个矩阵加速单元,其每一个 cycle 执行一个外积计算。在 1636 中，外积单元是 8*8的双精度乘累加矩阵。因此，每一个 cycle 实现 128Flops。在全芯片 SME 全力使用的情况下，CPU 可以工作在 1.55GHz，因此，整芯片的向量算力是 128Flops/cycle/core*1.55GHz*304Core=60TFlops。混合精度配比是 FP64:FP32:FP16:int8=1:2:4:16。```


## SME指令集参考

https://developer.arm.com/documentation/ddi0602/2021-09/SME-Instructions?lang=en
